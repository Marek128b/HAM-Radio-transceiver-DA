<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Button zum Auswählen des Geräts -->
    <Button
        android:id="@+id/selectDeviceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gerät auswählen" />

    <!-- ListView zum Anzeigen der Geräte -->
    <ListView
        android:id="@+id/deviceListView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginTop="16dp" />

    <!-- Button zum Verbinden -->
    <Button
        android:id="@+id/connectButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Verbinden" />

    <!-- Button zum Senden der Nachricht "Hello World" -->
    <Button
        android:id="@+id/sendButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send Hello World"
        android:layout_marginTop="16dp"
        android:layout_gravity="center" /> <!-- Button wird zentriert -->

</LinearLayout>
